const auth = require('./auth');
const places = require('./places');
const organizations = require('./organizations');
const storage = require('./storage');

module.exports = { auth, places, organizations, storage };
