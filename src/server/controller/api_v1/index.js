const auth = require('./auth');
const places = require('./places');
const organizations = require('./organizations');
const events = require('./events');
const users = require('./users');

module.exports = { auth, places, organizations, events, users };
